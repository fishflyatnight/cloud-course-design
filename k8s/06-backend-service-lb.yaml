apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: cloud-course
  annotations:
    kubernetes.io/elb.class: union
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 5000

